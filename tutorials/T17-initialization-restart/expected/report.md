# Expected engineering report

Record the 8° parent Case identity and why it was accepted. For each 12° branch, report initialization type, parent provenance when applicable, startup state bounds, accepted pseudo-steps, wall time, final CL/CD, and field-difference evidence.

The conclusion must answer two separate questions:

- Did the initialization reduce computational work?
- Did every accepted path converge to the same target solution within the stated tolerance?

If cross-mesh interpolation is used, add target mesh identity, overlap/coverage evidence, post-transfer conservation checks, and any local overshoot or undershoot.
